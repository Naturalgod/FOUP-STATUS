# 운영 PostgreSQL. 비워두면 data/foup_manager.db(SQLite)를 사용합니다.
DATABASE_URL=postgresql+psycopg://foup_user:change-me@localhost:5432/foup_status

# 사내 실시간 FOUP API. 비워두면 데모 어댑터를 사용합니다.
FOUP_LIVE_API_URL=
FOUP_LIVE_API_TOKEN=
FOUP_LIVE_API_TIMEOUT=5

